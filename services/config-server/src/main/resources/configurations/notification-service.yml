spring:
  data:
    mongodb:
      username: glite
      password: GliteEcom$200
      host: localhost
      port: 27017
      database: notification
      authentication-database: admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: paymentGroup, orderGroup
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: 'org.amouri.ecommerce.*' # allow deserialization of classes in this package only
        spring.json.type.mapping: orderConfirmation:org.amouri.ecommerce.DTOs.OrderConfirmation,paymentNotificationRequest:org.amouri.ecommerce.DTOs.PaymentNotificationRequest

  mail:
    host: localhost
    port: 1025
    username: amouri
    password: Souhail123
    properties:
      mail:
        smtp:
          trust: '*'
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

server:
  port: 8040