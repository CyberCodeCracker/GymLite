spring:
  data:
    mongodb:
      uri: mongodb://glite:GliteEcom200@localhost:27017/notification?authSource=admin
  kafka:
    consumer:
      bootstrap-servers: localhost:9092
      group-id: notification-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: 'org.amouri.ecommerce.*'
        spring.json.type.mapping: orderConfirmation:org.amouri.ecommerce.DTOs.OrderConfirmation,paymentConfirmation:org.amouri.ecommerce.DTOs.PaymentConfirmation  # ✅ Fixed

  mail:
    host: localhost
    port: 1025
    username: amouri
    password: Souhail123
    properties:
      mail:
        smtp:
          trust: '*'
        auth: true
        starttls:
          enabled: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000

server:
  port: 8040